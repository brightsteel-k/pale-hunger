package com.br1ghtsteel.palehunger.entity.ai;

public interface IPaleHungerEntity {

    boolean isProvoked();
    void setProvoked(boolean newProvoked);

    boolean isRooted();

    void setRooted(boolean newRooted);
}
